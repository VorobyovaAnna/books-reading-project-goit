import React from 'react';

const GoogleRedirect = () => {
  //Collect token,name,email from query URL params and put
  // them in Redux state
  return <p>Google login succeeded</p>;
};

export default GoogleRedirect;
